# Server-only — used by the Route Handler proxy, never exposed to the browser
DJANGO_INTERNAL_API_URL=http://localhost:8000/api/v1

# Public — used only for absolute-URL edge cases (metadata, CSRF Referer synthesis)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
